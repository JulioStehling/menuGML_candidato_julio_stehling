//altera a cor do texto quando o botão é selecionado
if global.wid_selec == id {
	if !selec cor_atual = cor_texto_selec;
} else cor_atual		= cor_texto;

// Inherit the parent event
event_inherited();

